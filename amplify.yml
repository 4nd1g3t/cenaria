version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # Con output:'export', Next genera estáticos en /out
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*